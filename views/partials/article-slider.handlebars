<div id="{{articleTempId}}-slider" class="right-side-slider article-slider">

	<div class="article-slider-output">
		<p class="article-slider-publication">{{publication}}</p>
		<img class="article-slider-image" src="{{image}}">
		<h4 class="article-slider-title">{{title}}</h4>
		<p class="article-slider-byline">{{byline}}</p>
		<p class="article-slider-date">{{date}}</p>
		<p class="article-slider-summary">{{summary}}</p>
		<p class="article-slider-read-article"><a href="{{url}}">Go To Article &raquo;</a></p>
	</div>

	<button data-id="{{articleTempId}}" class="add-worthy-btn"> + WORTHY</button>

	<div id="{{articleTempId}}-data" type="hidden" data-publication="{{publication}}" data-url="{{url}}" data-headline="{{headline}}" data-section="{{section}}" data-subsection="{{subsection}}" data-title="{{title}}" data-byline="{{byline}}" data-summary="{{summary}}" data-date="{{date}}" data-image="{{image}}" data-imageLarge="{{imageLarge}}"></div>

</div>

<div id="{{articleTempId}}-slider-bg" class="article-slider-bg"></div>
